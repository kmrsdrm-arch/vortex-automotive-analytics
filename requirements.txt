# Core API Dependencies (Optimized for Vercel Serverless)
fastapi==0.104.1
pydantic==2.5.0
pydantic-settings==2.1.0

# Database
sqlalchemy==2.0.23
psycopg2-binary==2.9.9
alembic==1.12.1

# Data Processing (Essential only)
pandas==2.1.3
numpy==1.26.2
faker==20.1.0

# LLM (Minimal - no heavy deps)
openai==1.3.7
tiktoken==0.5.1

# HTTP Client
httpx==0.25.2

# Utilities
python-dotenv==1.0.0
python-multipart==0.0.6

# Note: Heavy dependencies removed for Vercel deployment:
# - uvicorn (not needed in serverless)
# - streamlit (deploy separately to Streamlit Cloud)
# - langchain, chromadb (too large for serverless)
# - development tools (black, isort, flake8, pytest)
# - alembic (database migrations - run locally)
# - faker (data generation - run locally)

